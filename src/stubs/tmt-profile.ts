// Editable stub payloads for src/tools/tmt-profile.ts.
//
// When USE_API_STUBS=true the tool file returns these constants instead of
// calling the TMT Profile API. Currently only the bearer token is stubbed; add
// per-endpoint stubs here as needed.

/** Bearer token returned by tmt-profile authenticate() in stub mode. */
export const TMT_PROFILE_AUTH_STUB = 'dummy';
